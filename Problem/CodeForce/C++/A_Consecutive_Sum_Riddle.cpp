#include <bits/stdc++.h>

using namespace std;

#define ll long long

int main(){
    // freopen("input.txt", "r", stdin);
    ll t, n;
    cin >> t;
    while (t--){
        cin >> n;
        cout << (-n+1) << " " << n << endl;
    }
    // fclose(stdin);
    return 0;
}